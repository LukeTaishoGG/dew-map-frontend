import { Box, IconButton } from '@mui/material';
import MenuOutlinedIcon from '@mui/icons-material/MenuOutlined';

export default function HumBarger() {
  return (
    <Box>
      <IconButton>
        <MenuOutlinedIcon />
      </IconButton>
    </Box>
  );
}